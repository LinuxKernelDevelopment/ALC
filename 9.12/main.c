#include <stdio.h>
#include "element.h"
#include "PQindex.h"

int main(void)
{
   int i;
   for(i = 0; i < 11; i++)  ITEMscanf(&data[i]);
   for(i = 0; i < 11; i++) ITEMshow(data[i]);
   PQinit();
   for(i = 0; i < 11; i++) PQinsert(i);
  test();
   return 0;
}
